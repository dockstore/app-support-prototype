# Docker compose formatted system.
version: "3"
services:
  hail:
    image: heliumdatastage/helx-hail:latest
    ports:
      - ${HOST_PORT}:8000
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 12G
        reservations:
          cpus: '2'
          memory: 12G
